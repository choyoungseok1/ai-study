# 문제 설명
# m개의 행과 n개의 열로 구성된 격자가 주어지며, 이는 사막 지도를 나타냅니다. 사막 지도의 가장 왼쪽 위칸 좌표는 (0, 0), 오른쪽 아래칸 좌표는 (m-1, n-1)입니다. 이 사막 어딘가에 가로 w, 세로 h 크기의 선인장 구역을 조성하려 합니다. 선인장 구역은 격자 축에 맞춘 연속된 w × h 크기의 부분 격자이며, 회전할 수 없습니다.

# 비구름은 미리 정해진 순서대로 격자의 여러 칸에 비를 뿌립니다. 이때 빗방울이 처음으로 선인장 구역에 포함된 칸에 떨어졌을 때, 그 시점을 선인장이 처음으로 비를 맞는 순간으로 기록합니다. 당신은 선인장이 가능한 한 늦게 비를 맞도록, 선인장 구역의 위치를 정하려고 합니다.

# 선인장이 비를 맞지 않도록 선인장 구역의 위치를 정할 수 있다면 해당 위치가 가장 우선됩니다.
# 가능한 늦게 비를 맞는 선인장 구역 후보가 여러 개라면 그중 가장 위쪽 행, 그래도 여러 개면 가장 왼쪽 열에 위치한 구역을 선택합니다.
# 격자의 세로 길이와 가로 길이를 나타내는 정수 m, n, 선인장 구역의 세로 길이와 가로 길이를 나타내는 정수 h, w, 그리고 빗방울이 떨어지는 순서대로 칸의 좌표를 담은 2차원 정수 배열 drops가 매개변수로 주어집니다. 주어진 조건을 만족하는 선인장 구역에 포함된 가장 왼쪽 위칸의 좌표를 정수 배열로 return 하도록 solution 함수를 완성해 주세요.

def solution(m, n, h, w, drops):
    answer = []
    for i in range((m-h+1)):
        answer.append([-1 for i in range((n-w+1))])
    for idx, (r, c) in enumerate(drops):
        for i in range(max(0, r-h+1), min(m-h, r) + 1):
            for j in range(max(0, c-w+1), min(n-w, c) + 1):
                if answer[i][j] == -1:
                    answer[i][j] = idx
    max_val = -1
    max_r,max_c = 0,0
    for a in range(len(answer)):
        for b in range(len(answer[0])):
            if answer[a][b] == -1 :
                return [a,b]
            if answer[a][b] > max_val:
                max_val = answer[a][b]
                max_r, max_c = a, b
    return [max_r,max_c] 



# 잘한 점:

# 문제 분해를 잘 했어 — 위치 배열 생성 → 영향 범위 계산 → 최적 위치 탐색으로 단계를 나눔
# max(0, r-h+1), min(m-h, r)+1 범위 계산 정확
# enumerate로 빗방울 순번 추적하는 거 깔끔
# -1 우선 → max값 탐색 순서도 문제 조건에 맞음
# 개선할 점:

# 변수명 i 중복 — 바깥 for문에서 for i in range((m-h+1))쓰고, 안쪽 리스트 컴프리헨션에서도 for i in range야. 지금은 스코프가 다르니까 돌아가긴 하는데, 헷갈리기 쉬워. 안쪽은 _로 바꾸는 습관 들여
# 배열 크기에 불필요한 괄호 — (m-h+1) 바깥 괄호 없어도 돼. 가독성만 떨어져
# 시간복잡도 — 이건 이미 알고 있지. drops × h × w가 핵심 병목. 나중에 imos법/2D 누적합으로 해결