# 문제 설명
# 문자열로 구성된 리스트 strings와, 정수 n이 주어졌을 때, 각 문자열의 인덱스 n번째 글자를 기준으로 오름차순 정렬하려 합니다. 예를 들어 strings가 ["sun", "bed", "car"]이고 n이 1이면 각 단어의 인덱스 1의 문자 "u", "e", "a"로 strings를 정렬합니다.

# 제한 조건
# strings는 길이 1 이상, 50이하인 배열입니다.
# strings의 원소는 소문자 알파벳으로 이루어져 있습니다.
# strings의 원소는 길이 1 이상, 100이하인 문자열입니다.
# 모든 strings의 원소의 길이는 n보다 큽니다.
# 인덱스 1의 문자가 같은 문자열이 여럿 일 경우, 사전순으로 앞선 문자열이 앞쪽에 위치합니다.

def solution(strings, n):
    answer = sorted(strings,key= lambda x : (x[n],x))

    return answer


# 피드백

# 잘한 점: sorted() + key 조합을 처음 쓴 거치고 빠르게 적용했어. 방향 전환도 좋았고 — 삽입 정렬 직접 구현하다가 "이건 아닌데" 감지한 게 중요해.

# 개선할 점: 처음에 빈 리스트에 하나씩 넣으면서 비교하려 한 건 정렬을 직접 구현하려는 접근이야. Lv1~2에서는 거의 안 나와. Python 내장 정렬이 있으면 쓰는 게 맞고, 핵심은 key를 어떻게 설계하느냐야.

# 패턴 하나 기억해: sorted()의 key에 튜플 반환하면 다중 기준 정렬.