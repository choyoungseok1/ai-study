# 문제 설명
# AI 엔지니어인 현식이는 데이터를 분석하는 작업을 진행하고 있습니다. 데이터는 ["코드 번호(code)", "제조일(date)", "최대 수량(maximum)", "현재 수량(remain)"]으로 구성되어 있으며 현식이는 이 데이터들 중 조건을 만족하는 데이터만 뽑아서 정렬하려 합니다.

# 예를 들어 다음과 같이 데이터가 주어진다면

# data = [[1, 20300104, 100, 80], [2, 20300804, 847, 37], [3, 20300401, 10, 8]]
# 이 데이터는 다음 표처럼 나타낼 수 있습니다.

# code	date	maximum	remain
# 1	20300104	100	80
# 2	20300804	847	37
# 3	20300401	10	8
# 주어진 데이터 중 "제조일이 20300501 이전인 물건들을 현재 수량이 적은 순서"로 정렬해야 한다면 조건에 맞게 가공된 데이터는 다음과 같습니다.

# data = [[3,20300401,10,8],[1,20300104,100,80]]
# 정렬한 데이터들이 담긴 이차원 정수 리스트 data와 어떤 정보를 기준으로 데이터를 뽑아낼지를 의미하는 문자열 ext, 뽑아낼 정보의 기준값을 나타내는 정수 val_ext, 정보를 정렬할 기준이 되는 문자열 sort_by가 주어집니다.

# data에서 ext 값이 val_ext보다 작은 데이터만 뽑은 후, sort_by에 해당하는 값을 기준으로 오름차순으로 정렬하여 return 하도록 solution 함수를 완성해 주세요. 단, 조건을 만족하는 데이터는 항상 한 개 이상 존재합니다.

def solution(data, ext, val_ext, sort_by):
    dic_data = {"code":0, "date":1, "maximum":2, "remain":3}
    filtered = [row for row in data if row[dic_data[ext]] < val_ext]
    sorted_data = sorted(filtered, key=lambda x: x[dic_data[sort_by]])
    return sorted_data
# 데이터 분석 문제 피드백

# 잘한 점:

# 딕셔너리로 컬럼명을 인덱스에 매핑한 것(dic_data)이 좋았음
# sorted에 lambda를 활용한 정렬은 맞았음
# 실수:

# 필터링을 값만 뽑아서 별도 리스트에 저장 → 다시 in으로 원본에서 찾으려 함. 값이 다른 컬럼에 중복되면 엉뚱한 행이 매칭됨
# 조건이 <인데 <=로 씀
# 배운 것:

# 2차원 리스트 필터링은 리스트 컴프리헨션 한 줄로 끝남: [row for row in data if row[idx] < val]
# 굳이 값만 뽑아서 다시 매칭하지 말고, 행 전체를 조건으로 걸러내는 게 정석
