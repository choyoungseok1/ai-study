# 어느 학교에 페인트가 칠해진 길이가 n미터인 벽이 있습니다. 벽에 동아리 · 학회 홍보나 회사 채용 공고 포스터 등을 게시하기 위해 테이프로 붙였다가 철거할 때 떼는 일이 많고 그 과정에서 페인트가 벗겨지곤 합니다. 페인트가 벗겨진 벽이 보기 흉해져 학교는 벽에 페인트를 덧칠하기로 했습니다.

# 넓은 벽 전체에 페인트를 새로 칠하는 대신, 구역을 나누어 일부만 페인트를 새로 칠 함으로써 예산을 아끼려 합니다. 이를 위해 벽을 1미터 길이의 구역 n개로 나누고, 각 구역에 왼쪽부터 순서대로 1번부터 n번까지 번호를 붙였습니다. 그리고 페인트를 다시 칠해야 할 구역들을 정했습니다.

# 벽에 페인트를 칠하는 롤러의 길이는 m미터이고, 롤러로 벽에 페인트를 한 번 칠하는 규칙은 다음과 같습니다.

# 롤러가 벽에서 벗어나면 안 됩니다.
# 구역의 일부분만 포함되도록 칠하면 안 됩니다.
# 즉, 롤러의 좌우측 끝을 구역의 경계선 혹은 벽의 좌우측 끝부분에 맞춘 후 롤러를 위아래로 움직이면서 벽을 칠합니다. 현재 페인트를 칠하는 구역들을 완전히 칠한 후 벽에서 롤러를 떼며, 이를 벽을 한 번 칠했다고 정의합니다.

# 한 구역에 페인트를 여러 번 칠해도 되고 다시 칠해야 할 구역이 아닌 곳에 페인트를 칠해도 되지만 다시 칠하기로 정한 구역은 적어도 한 번 페인트칠을 해야 합니다. 예산을 아끼기 위해 다시 칠할 구역을 정했듯 마찬가지로 롤러로 페인트칠을 하는 횟수를 최소화하려고 합니다.

# 정수 n, m과 다시 페인트를 칠하기로 정한 구역들의 번호가 담긴 정수 배열 section이 매개변수로 주어질 때 롤러로 페인트칠해야 하는 최소 횟수를 return 하는 solution 함수를 작성해 주세요.
def solution(n, m, section):
    answer = 0
    # sec = [0 if num in section else 1 for num in range(1,n+1)]
    
#     for i in range(len(sec)) :
#         if sec[i] == 0 :
            
#             if i+m > len(sec) :
#                 sec[i:] = [1]*(len(sec) - i) 
#                 answer +=1
#             else :
#                 sec[i:i+m] = [1]*m
#                 answer +=1
    # i = 0
    # while i < n:
    #     if sec[i] == 0:
    #         answer += 1
    #         i += m   # m칸 점프
    #     else:
    #         i += 1
    painted_end = 0  # 지금까지 칠한 가장 오른쪽 끝
    for s in section:
        if s > painted_end:  # 아직 안 칠해진 구역
            answer += 1
            painted_end = s + m - 1
    
    return answer

# 오늘 풀이 피드백
# 잘한 점

# 첫 코드에서 에러 났을 때 당황 안 하고 하나씩 고침 (슬라이스 할당, m+1 오프바이원)
# 힌트 주면 그대로 베끼지 않고 이해하려고 함 ("너 말은 이거지?" 하면서 본인 코드로 작성)
# 3가지 풀이 다 시도해본 거 좋음. 이게 진짜 학습임

# 개선할 점

# 첫 코드에서 자기 점검 부족했음

# sec[i:] = 1 — 이거 한 번이라도 직접 돌려봤으면 바로 에러 떴을 거야
# return 값이 sec인 것도 마찬가지. 제출 전에 한 번 돌려보는 습관 필수
# 학습 플랜의 "return 위치" "변수명 혼동" 패턴이 또 나왔어


# m과 m+1 헷갈림 — 탐색 범위 산정 실수

# 롤러 길이 m이면 인덱스로 [i:i+m]. 이건 감으로 풀지 말고 작은 예시로 직접 그려봐
# 예: m=3, i=0이면 0,1,2번 칸 칠하니까 [0:3] → [i:i+m]. 매번 이렇게 검산.


# 시간 초과 의심했을 때 진단이 약함

# "런타임 에러 같다"고 했는데 사실 시간 초과였잖아. 채점 메시지 정확히 읽기
# 코테에서 시간 초과 났을 때 떠올려야 할 것: in 검색이 list냐 set이냐, 이중 반복문인가, 불필요한 배열 생성하는가


# 세 번째 풀이를 처음에 떠올리지 못한 게 아쉬움

# "section이 정렬돼 있다"는 조건은 문제에 명시돼 있었어. 이런 조건은 항상 활용 힌트임
# 앞으로 문제 풀 때: 입력 조건에 "정렬됨", "중복 없음", "범위가 작음" 같은 게 있으면 그게 풀이의 단서