# 문제 설명
# 지민이는 다양한 크기의 정사각형 모양 돗자리를 가지고 공원에 소풍을 나왔습니다. 공원에는 이미 돗자리를 깔고 여가를 즐기는 사람들이 많아 지민이가 깔 수 있는 가장 큰 돗자리가 어떤 건지 확인하려 합니다. 예를 들어 지민이가 가지고 있는 돗자리의 한 변 길이가 5, 3, 2 세 종류이고, 사람들이 다음과 같이 앉아 있다면 지민이가 깔 수 있는 가장 큰 돗자리는 3x3 크기입니다.

# 10.jpg

# 지민이가 가진 돗자리들의 한 변의 길이들이 담긴 정수 리스트 mats, 현재 공원의 자리 배치도를 의미하는 2차원 문자열 리스트 park가 주어질 때 지민이가 깔 수 있는 가장 큰 돗자리의 한 변 길이를 return 하도록 solution 함수를 완성해 주세요. 아무런 돗자리도 깔 수 없는 경우 -1을 return합니다.

# 제한사항
# 1 ≤ mats의 길이 ≤ 10
# 1 ≤ mats의 원소 ≤ 20
# mats는 중복된 원소를 가지지 않습니다.
# 1 ≤ park의 길이 ≤ 50
# 1 ≤ park[i]의 길이 ≤ 50
# park[i][j]의 원소는 문자열입니다.
# park[i][j]에 돗자리를 깐 사람이 없다면 "-1", 사람이 있다면 알파벳 한 글자로 된 값을 갖습니다.


def solution(mats, park):
    answer = 0
    mats = sorted(mats,reverse = True)
    for i in mats:
        for r in range(len(park) - i + 1):
            for c in range(len(park[0]) - i + 1):
                if all(park[r+dr][c+dc] == "-1" for dr in range(i) for dc in range(i)):
                    return i
    return -1
# 잘한 점:

# 빈칸 기준으로 탐색해야 한다는 건 파악함
# 큰 것부터 시도해서 먼저 찾으면 바로 return하는 아이디어는 맞았음

# 실수:

# 중심점에서 퍼지는 방식으로 접근 → 짝수 처리 문제에 부딪힘. 시작점 기준으로 i×i 영역 체크가 훨씬 단순
# all() 안에서 행만 체크하고 열을 빠뜨림 → i×i 영역은 for dr in range(i) for dc in range(i)로 행열 모두 봐야 함
# return -1 위치를 for문 안에 넣어서 첫 시도 실패 시 바로 종료 — 이거 오늘만 3번째 반복된 실수

# 핵심 교훈:

# 2차원 영역 탐색은 "시작점 + 크기"로 접근하는 게 정석
# return -1은 모든 for문 밖에 놓기