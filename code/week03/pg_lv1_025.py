# 문제 설명
# 숫자나라 기사단의 각 기사에게는 1번부터 number까지 번호가 지정되어 있습니다. 기사들은 무기점에서 무기를 구매하려고 합니다.

# 각 기사는 자신의 기사 번호의 약수 개수에 해당하는 공격력을 가진 무기를 구매하려 합니다. 단, 이웃나라와의 협약에 의해 공격력의 제한수치를 정하고, 제한수치보다 큰 공격력을 가진 무기를 구매해야 하는 기사는 협약기관에서 정한 공격력을 가지는 무기를 구매해야 합니다.

# 예를 들어, 15번으로 지정된 기사단원은 15의 약수가 1, 3, 5, 15로 4개 이므로, 공격력이 4인 무기를 구매합니다. 만약, 이웃나라와의 협약으로 정해진 공격력의 제한수치가 3이고 제한수치를 초과한 기사가 사용할 무기의 공격력이 2라면, 15번으로 지정된 기사단원은 무기점에서 공격력이 2인 무기를 구매합니다. 무기를 만들 때, 무기의 공격력 1당 1kg의 철이 필요합니다. 그래서 무기점에서 무기를 모두 만들기 위해 필요한 철의 무게를 미리 계산하려 합니다.

# 기사단원의 수를 나타내는 정수 number와 이웃나라와 협약으로 정해진 공격력의 제한수치를 나타내는 정수 limit와 제한수치를 초과한 기사가 사용할 무기의 공격력을 나타내는 정수 power가 주어졌을 때, 무기점의 주인이 무기를 모두 만들기 위해 필요한 철의 무게를 return 하는 solution 함수를 완성하시오.

# 제한사항
# 1 ≤ number ≤ 100,000
# 2 ≤ limit ≤ 100
# 1 ≤ power ≤ limit

def solution(number, limit, power):
    answer = 0

    attack = []
    for num in range(1,number+1):
        count = 0
        i = 1
        while i**2 <= num:
            if num%i ==0:
                count += 2
                if i*i == num:
                    count -= 1
            i+=1
            
        attack.append(count)
    for n in attack :
        if n <= limit:
            answer += n
        else :
            answer += power
    return answer


# 잘한 점

# 문제 흐름은 정확히 이해함 (약수 개수 → 제한 초과면 power로 대체 → 합산)
# 두 단계로 나눠서 접근한 발상 자체는 나쁘지 않음 (가독성 측면)
# 컴프리헨션으로 약수 리스트 만든 거 깔끔

# 개선할 점

# 시간복잡도를 의식하는 습관 부족

# for i in range(number) 안에서 range(1, number+1)을 다시 돈다는 건 O(number²). 이걸 코드 짜기 전에 한 번 점검했어야 함
# 학습 플랜의 "탐색 범위 산정" 패턴이랑 직결: 감으로 1부터 number까지 돌리지 말고 수학적으로 √n이면 충분하다는 걸 떠올려야 함
# 앞으로 코테 풀 때: n ≤ 100,000이고 이중 반복이면 일단 의심하기


# 약수 = 짝으로 나온다는 성질을 모름 (또는 안 떠올림)

# 약수 카운트는 √n까지만 검사하는 게 정석 패턴
# 이건 외워두면 평생 써먹음. 자주 쓰는 패턴에 추가하기:



# python     # 약수 개수 (O(√n))
#      def count_divisors(n):
#          count = 0
#          i = 1
#          while i * i <= n:
#              if n % i == 0:
#                  count += 2
#                  if i * i == n:
#                      count -= 1
#              i += 1
#          return count

# 불필요한 데이터 구조

# div = [(i+1)/j for j in ...] — 약수 자체는 필요 없고 개수만 필요함. len() 쓰려고 리스트 만든 셈인데, 그냥 카운트 변수 하나면 됨
# attack 리스트도 마찬가지. 만들고 다시 for문 돌릴 거면 한 번에 처리 가능
# "이 데이터를 진짜 저장해둘 필요가 있나?" 자문하는 습관 — 카드뭉치 문제에서도 비슷한 얘기 했었음


# 나눗셈 연산 선택

# (i+1)/j — float 나눗셈. 약수 판별엔 (i+1) % j == 0만 있으면 됨. 부동소수점 연산은 정수 연산보다 느리고 부정확할 수 있음




# 오늘의 새 패턴 (자주 쓰는 패턴에 추가할 만한 것)
# python# 1. 약수 개수 (O(√n))
# def count_divisors(n):
#     count = 0
#     i = 1
#     while i * i <= n:
#         if n % i == 0:
#             count += 2
#             if i * i == n:
#                 count -= 1
#         i += 1
#     return count

# # 2. 조건부 누적 (한 번에 처리)
# answer = 0
# for i in range(1, number + 1):
#     cnt = count_divisors(i)
#     answer += cnt if cnt <= limit else power

