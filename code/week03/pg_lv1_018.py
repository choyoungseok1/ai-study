# 코니는 영어 단어가 적힌 카드 뭉치 두 개를 선물로 받았습니다. 코니는 다음과 같은 규칙으로 카드에 적힌 단어들을 사용해 원하는 순서의 단어 배열을 만들 수 있는지 알고 싶습니다.

# 원하는 카드 뭉치에서 카드를 순서대로 한 장씩 사용합니다.
# 한 번 사용한 카드는 다시 사용할 수 없습니다.
# 카드를 사용하지 않고 다음 카드로 넘어갈 수 없습니다.
# 기존에 주어진 카드 뭉치의 단어 순서는 바꿀 수 없습니다.
# 예를 들어 첫 번째 카드 뭉치에 순서대로 ["i", "drink", "water"], 두 번째 카드 뭉치에 순서대로 ["want", "to"]가 적혀있을 때 ["i", "want", "to", "drink", "water"] 순서의 단어 배열을 만들려고 한다면 첫 번째 카드 뭉치에서 "i"를 사용한 후 두 번째 카드 뭉치에서 "want"와 "to"를 사용하고 첫 번째 카드뭉치에 "drink"와 "water"를 차례대로 사용하면 원하는 순서의 단어 배열을 만들 수 있습니다.

# 문자열로 이루어진 배열 cards1, cards2와 원하는 단어 배열 goal이 매개변수로 주어질 때, cards1과 cards2에 적힌 단어들로 goal를 만들 있다면 "Yes"를, 만들 수 없다면 "No"를 return하는 solution 함수를 완성해주세요.

def solution(cards1, cards2, goal):
    i = 0  # cards1의 다음 사용할 위치
    j = 0  # cards2의 다음 사용할 위치

    for word in goal:
        if i < len(cards1) and cards1[i] == word:
            i += 1
        elif j < len(cards2) and cards2[j] == word:
            j += 1
        else:
            return "No"

    return "Yes"


# 카드뭉치 문제 피드백
# 잘한 점

# 두 포인터 발상을 힌트만 보고 거의 정확하게 구현함
# 코드가 깔끔함. 군더더기 없이 핵심만 있음

# 개선할 점

# 첫 풀이의 접근 방향이 틀렸음

# goal.index() + cards1.insert() 조합으로 풀려고 한 건, 문제의 "순서대로 한 장씩" 조건을 놓친 거야
# 코드 짜기 전에 문제 조건 중 제약을 먼저 정리하는 습관: "cards1, cards2는 위에서부터만 뽑을 수 있다" 한 줄 적어두고 시작했으면 첫 풀이 방향이 달랐을 거야


# 인덱스 범위 체크 누락

# cards1[i] 접근 전에 i < len(cards1) 체크 안 함
# if cards1은 "리스트가 비었나"를 보는 거지 "i 위치가 유효한가"가 아님. 둘을 헷갈림
# 인덱스로 접근하기 전엔 항상 범위 체크 — 이건 앞으로 모든 문제에서 반사적으로 나와야 함