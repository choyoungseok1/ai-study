"""
========================================================================
[프로그래머스 Lv2] 도넛과 막대 그래프
풀이: 조영석 | Day 55 (2026-06-02)
========================================================================

[문제 설명]
도넛(사이클), 막대(일자 경로), 8자(두 사이클이 한 점에서 결합) 모양의
단방향 그래프가 여러 개 있다.
이들과 무관한 '생성 정점'을 하나 만든 뒤, 각 그래프의 임의의 정점 하나로
향하는 간선을 연결했다. (그래프끼리는 서로 연결되지 않고, 생성 정점만 쏨)

간선 정보 edges가 주어질 때
  [생성한 정점 번호, 도넛 수, 막대 수, 8자 수] 를 return.

각 모양의 정의:
  - 도넛(크기 n): n개 정점, n개 간선. 하나의 사이클. 모든 정점 in=1, out=1.
  - 막대(크기 n): n개 정점, n-1개 간선. 일자 경로. 끝 정점은 out=0.
  - 8자(크기 n): 2n+1개 정점, 2n+2개 간선. 두 사이클이 중심에서 결합.
                 중심 정점은 out=2 (그리고 in=2).

제한사항:
  1 <= edges 길이 <= 1,000,000
  1 <= a, b <= 1,000,000  (정점 번호가 띄엄띄엄일 수 있음)
  그래프 수의 합 >= 2


[핵심 아이디어 — 그래프 모양을 그리지 말고 '차수'만 본다]
각 정점의 진입차수(in)/진출차수(out)만 세면 종류가 판별된다.
모양을 머릿속에 그릴 필요가 전혀 없다. O(E)로 끝.

판별 기준:
  - 생성 정점 : in == 0 and out >= 2
        (무관하게 새로 만든 점이라 아무도 가리키지 않음 -> in=0,
         모든 그래프로 간선을 쏘므로 out = 그래프 수 >= 2)
  - 막대 수   : out == 0 인 정점 수
        (막대 끝 정점은 더 나갈 데가 없음. 막대마다 정확히 1개)
  - 8자 수    : out == 2 인 정점 수
        (8자 중심만 out=2. 도넛/막대엔 out=2가 없고, 생성 정점은 먼저 걸러짐)
  - 도넛 수   : (생성 정점의 out) - 막대 수 - 8자 수
        (도넛은 모든 정점 in=1,out=1이라 '고유 특이점'이 없어 직접 못 셈 -> 빼서 구함)

전체 그래프 수 = 생성 정점이 쏜 간선 수 = outdeg[생성정점].


[중요한 함정 — 왜 8자 판별을 out 기준으로? (in이 아니라)]
처음엔 8자 중심을 'in==2 and out==2'로 판별했는데 일부 8자를 놓쳤다.
원인: 생성 정점이 하필 8자의 '중심'을 가리키면 그 중심의 in이 2->3이 되어
      in==2 조건에서 탈락 -> 8자로 안 세지고 도넛으로 새어 들어감.

교훈: 판별 기준은 '외부 영향에 변하지 않는 속성'을 골라야 한다.
  - in : 생성 정점이 가리키면 +1 되어 흔들림 (불안정한 기준)
  - out: 그래프 고유 구조라 외부에서 간선이 들어와도 안 변함 (견고한 기준)
  => out == 2 하나만으로 8자 중심을 안정적으로 식별.
========================================================================
"""

from collections import Counter


def solution(edges):
    indeg = Counter()
    outdeg = Counter()
    for a, b in edges:
        outdeg[a] += 1   # a에서 나가는 간선
        indeg[b] += 1    # b로 들어오는 간선

    nodes = set(indeg) | set(outdeg)   # 등장한 모든 정점 (차수 집계 후에 생성)

    donut = bar = eight = 0
    answer_node = 0
    for v in nodes:
        if indeg[v] == 0 and outdeg[v] >= 2:
            answer_node = v          # 생성 정점 (가장 먼저 판별 — in=0으로 구분)
        elif outdeg[v] == 0:
            bar += 1                 # 막대 끝 정점
        elif outdeg[v] == 2:
            eight += 1               # 8자 중심 (out=2, in은 무관)

    donut = outdeg[answer_node] - bar - eight   # 도넛은 전체에서 빼서 구함
    return [answer_node, donut, bar, eight]     # 순서: 생성, 도넛, 막대, 8자


"""
========================================================================
[피드백 — Claude]
========================================================================

# 잘한 점
1. '모양이 복잡하다'는 막막함을 차수(in/out) 문제로 환원했다.
   그래프 탐색(DFS/BFS) 없이 O(E) 차수 집계만으로 푸는 게 이 문제의 정답 의도.
   edges 100만 개 제약도 이 접근이라 통과.
2. 반례로 규칙을 검증하려는 습관이 좋았다.
   ('도넛이 막대를 가리키면?' 같은 시도) -> 규칙(그래프끼리 연결 안 됨,
   생성 정점만 쏨 / 막대는 사이클 아닌 일자)을 다시 읽어 스스로 교정.
3. Counter의 특성(없는 키 조회 시 0 반환)을 활용해 키 존재 검사를 생략.

# 막혔던 지점 (복기용)
1. nodes를 edges 순회 '전에' 만들어 빈 set이 됨 -> 순회 후로 이동.
   에러도 안 나고 조용히 전부 0을 반환해서 잡기 까다로운 버그.
   체크 습관: '이 변수를 만들 때 필요한 데이터가 이미 채워졌나?' (반복 실수 1번 순서)
2. return 순서를 문제 요구(생성, 도넛, 막대, 8자)와 다르게 둠 -> 형식 재확인.
3. (핵심) 8자를 'in==2 and out==2'로 판별 -> 생성 정점이 중심을 가리킨 8자를
   놓쳐 도넛으로 샘. -> 'out==2'로 변경.
   * 판별 기준은 외부 영향에 안 변하는 속성(out)을 골라야 한다는 교훈.

# 코드 평가
- 통과 코드로 문제 없음. O(E) 한 번 + 정점 순회 한 번이라 100만 간선도 여유.
- 변수명이 의도를 잘 드러냄. 생성 정점을 가장 먼저 판별(elif 순서)한 것도 정확.

# 패턴 서랍에 넣을 것
- 그래프 '모양 분류'는 종종 차수(in/out) 시그니처로 환원된다.
  각 모양마다 '고유한 차수 특이점'을 찾아 그 개수를 세는 것이 정석.
    · 막대 = out 0 (경로의 끝)
    · 8자  = out 2 (두 사이클의 결합점)
    · 도넛 = 특이점 없음 -> 전체에서 빼서 계산
- 판별 기준은 '외부 간선 추가에도 불변인 속성'을 선택할 것 (in보다 out).
- Counter 두 개로 in/out 분리 집계, set(indeg)|set(outdeg)로 전체 정점 수집.
========================================================================
"""
