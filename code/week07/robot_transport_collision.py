"""
========================================================================
[프로그래머스 Lv2] 자동 운송 시스템 (로봇 충돌 시뮬레이션)
풀이: 조영석 | Day 55 (2026-06-02)
========================================================================

[문제 설명]
물류 센터에 n개의 포인트(좌표)가 있고, x대의 로봇이 정해진 경로를 따라
0초에 동시에 출발한다.

이동 규칙:
  - 로봇은 1초마다 r 또는 c 좌표를 1만큼 증가/감소시켜 이동한다.
  - 다음 포인트로 갈 때는 항상 '최단 경로'로 이동한다.
  - 최단 경로가 여러 개면, r이 변하는 이동을 c가 변하는 이동보다 '먼저' 한다.
    => 즉 경로가 유일하게 결정된다: r을 목표까지 다 맞춘 뒤 c를 맞춤.
  - 마지막 포인트에 도착하면 센터를 벗어난다 (그 뒤 경로는 고려 안 함).

위험 상황:
  - 어떤 시각에 같은 좌표에 로봇이 2대 이상 모이면 그 좌표는 '위험'.
  - 한 시각에 여러 좌표에서 위험이 발생하면 그 횟수를 모두 더한다.
    (한 좌표에 3대가 모여도 그 좌표는 +1 — '위험한 좌표의 개수'를 센다)

목표: 모든 로봇이 운송을 마칠 때까지 발생한 위험 상황의 총 횟수를 return.

제한사항:
  2 <= n <= 100,  1 <= r,c <= 100  (같은 좌표에 두 포인트 없음)
  2 <= x(로봇 수) <= 100
  2 <= m(경로 길이) <= 100,  모든 routes[i] 길이 동일
  routes[i][j]는 '포인트 번호'(1~n) — 0-index로 쓰려면 -1 필요


[핵심 아이디어 — 완전 시뮬레이션]
제약이 작다(n,x,m,좌표 모두 <=100). 경로 총 길이는 길어야 수천.
=> 각 로봇의 '시간별 좌표'를 전부 만들어 두고, 매 시각 충돌을 직접 센다.

1) 경로 생성: route의 인접한 두 점마다 'r 먼저, c 나중'으로 1칸씩 이동하며
   매 칸의 좌표를 기록. 시작점은 시각 0으로 먼저 한 번 넣는다.
   각 구간은 '이동분만' 기록하므로 구간 끝점=다음 시작점이 중복되지 않는다.

2) 충돌 체크: 시각 t를 0 ~ (가장 긴 경로 길이)까지 돌리고,
   t < len(path)인 로봇만(=아직 운행 중) 좌표를 모은다.
   Counter로 좌표별 대수를 세서 2대 이상인 좌표의 '개수'를 누적한다.

[왜 이렇게 세는가 — 처음 떠올린 'or 쌍 비교'가 안 되는 이유]
  A==B or B==C ... 방식은 (1) True/False만 줘서 '몇 번'을 못 세고,
  (2) 로봇이 100대면 쌍이 C(100,2)=4950개라 확장 불가.
  => 좌표별로 대수를 세고(Counter) '2대 이상인 좌표 수'를 더하는 방식이
     '여러 좌표 위험을 모두 더한다'와 '한 좌표에 여러 대=+1'을 동시에 처리.

[도착한 로봇 처리]
  경로 길이가 다른 = 도착 시각이 다른 것. 규칙상 도착한 로봇은 사라진다.
  매 시각 t에서 'if t < len(path)'로 운행 중인 로봇만 보면 자동 처리된다.
  (마지막 좌표에 계속 머문다고 처리하면 틀림)
========================================================================
"""

from collections import Counter


def solution(points, routes):
    all_paths = []   # 모든 로봇의 시간별 좌표 경로

    # 1) 각 로봇의 경로(시간별 좌표) 생성
    for route in routes:
        r, c = points[route[0] - 1]   # 시작 포인트 (번호 -> 인덱스: -1)
        path = [(r, c)]               # 시각 0 = 시작점 먼저 기록
        for j in range(len(route) - 1):
            r2, c2 = points[route[j + 1] - 1]   # 다음 포인트
            # r을 목표까지 1칸씩 (r 먼저)
            while r != r2:
                r += 1 if r2 > r else -1
                path.append((r, c))
            # c를 목표까지 1칸씩 (c 나중)
            while c != c2:
                c += 1 if c2 > c else -1
                path.append((r, c))
        all_paths.append(path)

    # 2) 매 시각 충돌 체크
    answer = 0
    max_t = max(len(p) for p in all_paths)   # 가장 긴 경로 길이
    for t in range(max_t):
        positions = []
        for p in all_paths:
            if t < len(p):              # 그 시각에 아직 운행 중인 로봇만
                positions.append(p[t])
        counter = Counter(positions)    # 좌표별 로봇 대수
        answer += sum(1 for cnt in counter.values() if cnt >= 2)  # 위험 좌표 수 누적

    return answer


"""
========================================================================
[피드백 — Claude]
========================================================================

# 잘한 점
1. 큰 흐름(경로 생성 -> 시간별 충돌 체크)을 처음부터 맞게 잡았다.
2. '최단 경로가 여러 개'라는 고민을 규칙 재독해로 스스로 해소.
   (규칙 4 'r 먼저 c 나중'이 경로를 유일하게 고정한다는 점)
3. 한 구간 이동 로직(while로 r 맞추고 c 맞추기)을 깔끔히 구현하고
   작은 예제로 검산까지 함: (1,1)->(3,2) => [(1,1),(2,1),(3,1),(3,2)].
4. 충돌 카운트를 'or 쌍 비교'에서 'Counter로 좌표별 집계'로 전환.
   확장성(로봇 100대)과 '여러 좌표 위험 합산'을 동시에 해결.

# 막혔던 지점 (복기용) — 전부 들여쓰기/누적 관련
이 문제에서 가장 많이 걸린 건 알고리즘이 아니라 '구조'였다.
  (1) 시작점을 path에 안 넣어 시각 0이 빠짐 -> 시작점 먼저 append.
  (2) 포인트 '번호'를 인덱스로 그대로 써서 한 칸 밀림 -> -1 필요.
  (3) 충돌 체크(counter/answer)가 for 루프 '밖'에 있어 마지막 시각만 계산됨.
  (4) 다시 for p '안'으로 들어가 로봇마다 중간 집계됨.
  (5) answer = (덮어쓰기) 로 매 시각 결과가 날아감 -> answer += (누적).

  => 반복 실수 패턴 6번(들여쓰기 레벨)과 4번(덮어쓰기 vs 누적)이 한 문제에서
     연달아 나왔다. '이 줄이 어느 루프 안에 속해야 하는가'를 쓰기 전에
     한 번 정하는 습관이 필요하다. 골격(주석)을 먼저 깔고 채우면 줄어든다.

  체크 습관:
   - 집계/누적 줄은 '무엇을 다 모은 뒤' 실행돼야 하나? -> 그 루프 '밖'.
   - 시각마다 더해야 하나? -> 시각 루프 '안' + '+=' 누적 (초기화는 루프 전 0).

# 코드 평가
- 통과 코드로 문제 없음. 시뮬레이션 규모(경로 수천 x 로봇 100)도 여유.
- 변수명(all_paths, positions, counter)이 의도를 잘 드러낸다.
- 굳이 더: 경로 생성에서 while 대신 for _ in range(abs(차이))도 가능하지만
  현재 while 방식도 방향 자동 처리(+1/-1)라 깔끔하므로 유지해도 좋다.

# 패턴 서랍에 넣을 것
- '시간별 위치를 전부 펼친 뒤 매 시각 검사'하는 완전 시뮬레이션:
  제약이 작을 때(좌표/개수 <=100) 가장 단순하고 확실한 접근.
- 여러 객체의 '동시 충돌'은 쌍 비교(O(x^2))가 아니라
  Counter로 위치별 집계 -> 임계치 이상 카운트. (확장성 + 다중 집계)
- 길이가 다른 시퀀스들을 시각 정렬: max 길이까지 t를 돌리고
  'if t < len(seq)'로 유효 구간만 참여시킨다.
========================================================================
"""
