# 2026-04-20 TIL

## 오늘 한 것
- 오늘은 code/ 폴더에 새로운 변경 없음
- Notion 캘린더 계획: 점프투파이썬 6장 + 클래스/모듈 심화 + 프로그래머스 3문제 → 미착수
- (참고) 어젯밤 늦게 작업한 파일이 4/19 TIL에 미반영되어 있음:
  - `pg_lv0_041.py` — WASD 방향키로 n값 변경 (if-elif 풀이 + count() 풀이 + dictionary 풀이 비교)
  - `pg_lv0_043.py` — 홀수/짝수 이어붙인 수의 합 (문자열 연결 + eval → int() 리팩터링)
  - `pg_lv0_057.py` — 문자열 구간 뒤집기 (리스트 변환 + 슬라이싱 `[::-1]`, 정답 참조)
  - `pg_lv0_058.py` — 빈 파일 (미착수)

## 배운 개념
- (어젯밤 작업 기준) `str.count()` 활용하면 반복문 없이 문자 빈도 계산 가능
- (어젯밤 작업 기준) dictionary를 매핑 테이블로 활용하면 if-elif 체인을 깔끔하게 대체 가능
- (어젯밤 작업 기준) `eval()` 대신 `int()`로 형변환하는 게 더 안전하고 간결
- (어젯밤 작업 기준) 리스트 슬라이싱 `s_list[s:e+1] = s_list[s:e+1][::-1]`로 구간 뒤집기 — `reverse()`는 원본을 바꾸지만 임시 리스트만 생성되는 함정 주의

## 막힌 부분
- (비워둠 - 직접 채움)

## 내일 할 것
- requests + BeautifulSoup 크롤러 튜토리얼 + 프로그래머스 3문제 (Notion 캘린더 기준)
- 오늘 미진행분(점프투파이썬 6장 + 클래스/모듈 심화 + 프로그래머스 3문제)도 함께 소화 필요
