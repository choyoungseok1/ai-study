# 2026-05-05 TIL

## 오늘 한 것
- `code/week03/pg_lv1_018.py`: 프로그래머스 Lv.1 카드뭉치 문제 — 두 카드뭉치에서 순서대로만 뽑을 수 있는 조건을 두 포인터(i, j)로 구현, goal 배열 순회하며 매칭
- `code/week03/pg_lv1_020.py`: 프로그래머스 Lv.1 개인정보 수집 유효기간 문제 — 모든 날짜를 일(day) 단위 정수로 변환(y×336 + m×28 + d)하여 비교, 약관별 유효기간을 dict로 관리
- `code/week03/pandas_review.ipynb`: Pandas 실전 연습 (Chipotle 주문 데이터) — 조건 필터링(loc, 불리언 인덱싱), 문자열 처리(str.replace → astype), 결측치 처리(fillna), groupby 집계, 정렬(sort_values), 값 치환(replace) 등 20문제 풀이

## 배운 개념
- **두 포인터 패턴**: 두 시퀀스를 순서 유지하며 합치는 문제 → 각각 인덱스를 두고 앞에서만 소비
- **인덱스 범위 체크 습관**: `list[i]` 접근 전 반드시 `i < len(list)` 확인 — `if list`(비었는지)와 혼동 주의
- **날짜 비교 → 단위 통일**: 복잡한 날짜 비교는 가장 작은 단위(일)로 변환 후 정수 비교
- **변수명 충돌 주의**: 함수 매개변수와 내부 변수가 같은 이름이면 덮어쓰기 발생 (to_days의 d)
- **Pandas 핵심 메서드 복습**: `value_counts()`, `nunique()`, `groupby().sum()/mean()`, `fillna()`, `replace()`, `sort_values()`

## 막힌 부분
- 

## 내일 할 것
- 
