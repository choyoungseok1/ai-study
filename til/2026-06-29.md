# 2026-06-29 TIL

## 오늘 한 것
- **새로 추가/변경된 코드 파일 없음.** 마감 기준 구간(06-28 오후 ~ 06-29 새벽)에 `code/` 폴더에서 변경·추가된 파일이 감지되지 않았다.
- `code/` 내 가장 최신 파일은 `code/week11/hotel.py`(06-26 작업물, 프로그래머스 Lv2 "호텔 대실" — sweep line으로 최대 동시 구간 = 최소 객실 수 풀이)이며, 이미 06-26 TIL에 정리 완료된 상태다.
- 참고: `git status`에 다수 파일이 수정된 것으로 표시되나, 전부 줄바꿈(CRLF/LF)·공백 정규화 변경이다(`git diff --ignore-all-space` 결과 비어 있음). 실제 코드 내용 변경 아님.

## 배운 개념
- (오늘 신규 코드 구현이 없어 새로 정리할 개념 없음.)
- 직전 학습 흐름 복기: RAG 고도화(rerank/HyDE) → RAGAS 평가(faithfulness·context) → 통합 파이프라인으로 이어지는 라인. 알고리즘 쪽은 sweep line / 구간 겹침 카운팅(호텔 대실)까지 진행됨.

## 막힌 부분
- (오늘 구현이 없어 코드상 막힌 지점 없음.)
- 절차상 메모: 학습 캘린더 기준 06-26("QLoRA 결과 확인 + RAGAS 평가 시작"), 06-27("RAGAS 마무리 + rag_app 평가 루프"), 06-29("RAG 통합·정리") 항목이 모두 **시작 전** 상태로 남아 있고 대응 코드 산출물이 `code/`에 없다. 실제 학습을 했다면 커밋/파일 저장이 누락됐을 수 있으니 검토 필요.

## 내일 할 것
- (Notion 학습 캘린더 06-30 항목 기준)
- **논문 1편 (DoRA 또는 RAG 평가 계열) + 리뷰 + Lv2 1문제** (Week 11).
- 더불어 밀려 있는 **RAG 통합·정리(검색→재정렬→평가 한 흐름, 포트폴리오용)** 와 **RAGAS 평가 루프 `rag_app` 연결** 분량을 함께 끌고 가기.
