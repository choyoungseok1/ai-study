# 2026-06-18 TIL

## 오늘 한 것
- **`code/week09/maze_escape.py`** — 프로그래머스 Lv2 "미로 탈출" BFS
  - "S→레버(L)→출구(E)" 여정을 **두 개의 독립 BFS(S→L, L→E)로 분해**하고 `d1 + d2` 합산 (도달 불가 시 -1)
  - `visited`를 `-1` 초기화로 **거리 저장 + 방문 체크 겸용**, S/L/E 좌표는 BFS 밖에서 1회만 탐색
  - 검증: 공식 예제 2개(16, -1) + 랜덤 격자 3만 개를 (좌표+레버여부) 상태공간 BFS와 전수 대조 → 불일치 0
- **`code/week09/rag_app.py`** — RAG 미니앱 통합 (Day 65 파이프라인을 실행형으로 묶음)
  - 인덱싱: 문서 → `RecursiveCharacterTextSplitter` → multilingual MiniLM 임베딩 → Chroma
  - 생성: `{context, question} | prompt | ChatGroq(llama-3.3-70b) | StrOutputParser` LCEL 체인
  - 질문 루프: 입력 → 답변 + 근거 청크(출처) 동시 출력

> ⚠️ **기록 동기화 메모**: 위 두 파일은 실제로 **6/17 저녁(KST)에 작성·커밋(Day 70)**됐는데, 6/17 TIL이 그날 오후 자동 실행된 뒤에 작업돼 어제 기록에서 누락됨 → 오늘 TIL에 반영함.
>
> ⚠️ **오늘(6/18) 계획 미착수**: Notion 6/18 항목은 **"버퍼데이 (Week 9 회고 + 논문)" (상태: 시작 전)**인데, 회고/논문 산출물이 감지되지 않음. 위 작업은 내용상 **6/17 항목 "여유/통합일 (RAG 미니앱 통합) + Lv2 1문제"** 에 해당하며, 그 항목은 Notion에서 이미 **완료** 처리돼 있음.

## 배운 개념
- **상태가 바뀌는 경로 문제**(레버 전/후)는 `visited` 하나로 풀면 꼬임. 해법 두 가지: ① **여정을 단계로 분해**해 각 단계가 독립 `visited`를 갖게 하기(이번 풀이), ② (좌표 + 레버여부)를 묶은 **3D-visited BFS**. 둘 다 익혀둘 것
- **LCEL 체인 조립법**: dict로 `context`/`question`을 병렬 주입, `retriever | format_docs`로 Document 리스트 → 문자열 변환, `RunnablePassthrough()`로 원 질문을 그대로 흘려보냄
- 한국어 말뭉치 RAG에는 영어 전용이 아닌 **multilingual 임베딩**(paraphrase-multilingual-MiniLM-L12-v2)을 써야 검색 품질이 유지됨

## 막힌 부분
- 미로: 처음에 `visited` 1개로 풀려다 "S→E→L→E"가 가능해 막힘 → 단계 분해로 재구성. 빠르게 짤 때 **Python2 잔문법**(매개변수 튜플 언패킹, 할당문 끝 콜론) 2건 SyntaxError → "이거 Py3 맞나" 점검 습관 필요
- **노트북 미저장 패턴 지속**: `Chroma.ipynb` / `React+Agentic_RAG.ipynb` / `LangChain.ipynb` 여전히 0바이트. "구현 = 저장까지" 가 또 안 지켜짐 → 복습 자산 누적 0
- **RAGAS 평가기 미완 이월**: faithfulness만 구현, answer relevance / context relevance 2종 미구현

## 내일 할 것
- **Notion 캘린더에 6/19 항목 없음** — Week 9 버퍼데이(6/18)가 마지막 계획 항목이고 Week 10은 아직 미계획 상태. 다음 주차 일정부터 채워넣을 것
- **최우선 이월: 오늘(6/18) 버퍼데이 본 작업** — Week 9 회고 + 논문 1편 (오늘 미착수분)
- 밀린 노트북 3개 채워 **저장**(체크리스트 적용) + RAGAS **answer/context relevance** 마저 구현해 3종 세트 완성
