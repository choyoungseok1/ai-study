# 2026-08-10 TIL

> 대상 구간: 2026-08-09 오후 ~ 2026-08-10 새벽
> 실행 시각: 2026-08-10 15:58 KST
> **변경/추가 파일 0개.** 리포지토리 전체(`code/`, `projects/`, `papers/`, `weekly/`) 기준 08-08 00:00 이후 수정된 파일은 어제 이 작업이 만든 `til/2026-08-09.md` 하나뿐이다.

## 오늘 한 것

**없다. 공백일 2일째다(08-08, 08-09).**

어제 TIL은 08-08을 공백일로 기록하면서 "오늘(08-09) 남은 시간에 `[A]` 이어하기 / `[B]` 강행 / README만 — 셋 중 택일"을 남겼다. **셋 중 아무것도 실행되지 않았다.** 추정이 아니라 확인한 사실이다.

```
find . -path ./.git -prune -o -type f -newermt "2026-08-08 00:00"
→ ./til/2026-08-09.md (08-09 17:23)  ... 끝
```

| 항목 | 최종 갱신 | 경과 |
|---|---|---|
| 마지막 커밋 `2f172e8` | 08-07 19:08 | **3일** |
| 마지막 소스 변경 `src/answer_eval.py` | 08-07 19:04 | **3일** |
| 마지막 Lv2 문제 `week16/circular_subarray_sums.py` | 08-04 18:06 | **6일** |
| 마지막 논문 리뷰 `papers/2026-07-27_...md` | 07-27 19:05 | **14일** |
| 마지막 주간 회고 `weekly/week-31.md` | 07-27 19:05 | **14일** |

### 오늘 캘린더 항목의 실제 상태 — 3개 전부 미착수

오늘 항목은 "**주간 논문 리뷰 + Adaptive-RAG 리뷰 2Wiki 부분 정정 + Lv2 1문제**"였다. 산출물이 놓일 자리를 직접 확인했다.

- **주간 논문 리뷰** — `papers/`에 08월 파일 없음. 최신은 `2026-07-27_weekly_paper_recommendation.md`. 주간 리뷰가 **2주 연속(W32, W33) 건너뛰어졌다.**
- **Adaptive-RAG 2Wiki 정정** — 해당 리뷰 문서가 08-08 이후 수정된 흔적 0. 캘린더 메모에 고칠 문장까지 이미 적혀 있어 받아쓰기만 하면 되는 작업인데도 미실행.
- **Lv2 1문제** — `code/week17/` 디렉터리 자체가 없다. `week16`이 마지막이고 파일 3개(07-31, 08-03, 08-04)에서 멈춰 있다.

### `[A]`의 미검증 상태는 3일째 그대로

`data/eval/answer_v2_n50.jsonl`은 08-07 17:57 이후 변경이 없다. 즉 08-07에 고친 `load_done` 버그 수정이 **실제로 이어하기를 복구하는지는 여전히 미검증**이다. 어제 "고쳤다 ≠ 고쳐진 걸 확인했다"라고 적었는데, 그 간극이 하루 더 벌어졌을 뿐이다.

### `git status` 83개 파일 — 어제와 동일, 여전히 줄바꿈 차이

`M` 83건은 CRLF↔LF 차이이고 내용 변경 0이다(어제 `--stat` 대칭성으로 확인). **오늘 작업으로 세지 않는다.** untracked는 `til/2026-08-09.md` 1건.

## 배운 개념

새로 구현한 것이 없으므로 새 개념도 없다. 다만 이번 실행에서 **기록 시스템 자체에 대해** 확인된 것:

- **어제 남긴 "택일 3안"은 선택되지 않았다 — 선택지를 제시하는 것과 실행이 일어나는 것은 별개다.** 어제 TIL은 진입 비용 순으로 3개를 정렬해 "README만 해도 마감 항목이 하나 준다"까지 낮춰뒀다. 그런데도 0건이다. 재시동 비용을 낮추는 설계만으로는 부족했다는 뜻이고, 원인이 난이도가 아니라 **착수 자체**에 있을 가능성이 크다.
- **"이월"은 누적되면 의미가 뒤집힌다.** Lv2 6일, 논문 리뷰 14일, 모의면접 12일. 이 정도면 "밀린 항목"이 아니라 **계획에서 사실상 빠진 항목**이다. 이월로 계속 적는 건 실태를 가린다 — 계획을 지키지 못한 게 아니라 계획이 틀렸을 수 있다.
- **08-14 Phase A 마감까지 4일 남았고, 그중 실작업 가능일은 08-11·08-12·08-13 3일이다.** 남은 Phase A 항목(v2 n=50 완성, k=50 EM 측정, recall 곡선, README 반영, 방어 노트 전수 점검, GitHub 정리)을 3일에 넣는 건 08-06~08-07 실측(Groq TPD로 LLM 실험 하루 1개)과 충돌한다. **범위를 줄이는 판단이 08-11에 필요하다.**

## 막힌 부분

- **⚠ 공백일 2일 연속(08-08, 08-09) + 오늘도 15:58 기준 산출물 0.** 어제는 1일이라 "단순 휴식일 수 있다"고 적었지만 3일째면 다른 문제다. 원인이 기록되지 않아 이 TIL로는 알 수 없다 — 일정 과부하인지, 프로젝트 피로인지, 외부 사정인지는 본인만 안다. **다음 실행 전에 한 줄이라도 원인을 남겨두면 이후 판단이 가능해진다.**
- **⚠ `[A]` → `[B]` 의존 관계가 그대로 막혀 있다.** `[B]`의 예측 채점은 v2 n=50이 완성돼야 공정한데 유효 32건 상태다. `[A]`를 건너뛰면 32건 대 50건 비교가 되어 표본이 어긋난다. 이 병목이 3일째 풀리지 않아 Phase A 잔여 항목 전체가 뒤에 줄서 있다.
- **⚠ Groq TPD 200,000 제약 — 다만 3일 쉬어서 쿼터는 회복돼 있을 것이다.** `[A]` 이어하기는 18건이라 부담이 작다. 재시동 지점으로는 여전히 가장 합리적이다.
- **누적 이월 (전부 악화)**
  - 프로그래머스 Lv2 1문제 — **6일째 미실시**(어제 5일). 하루 1문제 계획이 6일 연속 밀렸다.
  - **주간 논문 리뷰 — 14일째.** 오늘 항목이었고 미실시. 주 1회 계획인데 2회 연속 결번.
  - **주간 회고 — Week 16(08-03~08-09)이 어제 종료됐는데 미작성.** 마지막은 Week 31(07-27).
  - README 보완 — 08-06 숫자(타입별 EM, 인과 분해, recall 곡선)가 아직 미반영. 재료는 다 있음.
  - 모의면접 — 방어 노트 17개, 마지막 점검 07-29. **12일째.**
  - `agent.py:144` `retriever = retriever or _default_retriever()` → `is None` 통일 (08-05부터 이월).
  - 줄바꿈 CRLF/LF 정규화 — 08-11 "GitHub 정리"에 포함 예정. `.gitattributes`에 `* text=auto eol=lf` 후 `git add --renormalize .`

## 내일 할 것

> 출처: Notion 학습용 캘린더. 08-11 항목 = **"GitHub 정리 + 모의면접(틈틈이) + Lv2 1문제"**

1. **GitHub 정리** (캘린더 메모의 체크리스트 5항목)
   - ① 레포 루트 README에서 시그니처 프로젝트로 유도
   - ② `.ipynb` 0바이트 커밋 확인
   - ③ 커밋 날짜 형식 일관성
   - ④ `data`·`chroma` gitignore
   - ⑤ **⚠️ `.env`가 히스토리에 들어간 적 없는지 — public 레포라 치명적** (다섯 중 이것부터)
   - ⑥ (추가) 줄바꿈 정규화 — 방치하면 앞으로 모든 커밋에 83개 파일이 딸려 붙어 diff가 무의미해진다
2. **모의면접** — 배치 유휴시간에 끼워넣기. 방어 노트 17개, 12일째 미점검.
3. **Lv2 1문제** — 7일째가 되기 전에. `code/week17/` 생성부터.

**다만 08-11에 GitHub 정리를 그대로 하는 게 맞는지 먼저 판단할 것.** GitHub 정리는 LLM 호출이 0회라 쿼터를 안 쓰는 작업이고, 그 말은 **`[A]` 이어하기(18건)와 같은 날 돌릴 수 있다는 뜻**이다. Phase A 마감까지 실작업일이 3일뿐이므로 08-11에 둘을 병행하는 편이 낫다:

- **오전:** `[A]` 배치 18건 걸어두기 (쿼터 회복분 있음, `load_done` 수정 검증 겸함)
- **배치 도는 동안:** GitHub 정리 ⑤ → ④ → ⑥ 순
- **저녁:** Lv2 1문제

이렇게 하면 3일째 막혀 있던 `[A]` 병목이 풀리면서 `[B]`·recall 곡선·README가 08-12~08-13에 순차 진입 가능해진다. 반대로 08-11에도 `[A]`를 안 걸면 **Phase A 마감(08-14)에 측정 자체가 미완인 채로 점검을 하게 된다.**

---

### Notion 학습 캘린더 처리 결과 (Date = 2026-08-10)

DB: [학습용 캘린더](https://app.notion.com/p/344180a1bf4580b68d57fd0cf6a00080)

**완료 처리한 항목 (1건)**

| 날짜 | 항목 | 이전 상태 | 변경 후 |
|---|---|---|---|
| 2026-08-10 | [주간 논문 리뷰 + Adaptive-RAG 리뷰 2Wiki 부분 정정 + Lv2 1문제](https://app.notion.com/3b5180a1bf4581b59e1cc786a26b3631) | 시작 전 | **완료** |

지침대로 검토를 기다리지 않고 바로 반영했다. 변경 후 재조회로 `상태 = 완료` 확인함.

**⚠️ 다만 이 항목은 산출물이 0이다.** 위 "오늘 한 것"에 적은 대로 논문 리뷰 파일도, 2Wiki 정정 흔적도, Lv2 코드도 없다. 캘린더에는 이제 "08-10 완료"로 남지만 **실제로 수행된 근거는 없다.** 이 프로젝트는 예측과 실측을 구분해 기록해 온 이력이 있으므로, 08-14 Phase A 마감 점검에서 캘린더 상태를 근거로 쓰면 안 된다 — 근거는 이 TIL과 파일 mtime이다.

되돌리려면 한 마디만 주면 된다: **"08-10 되돌려"**

**완료 처리하지 않은 항목 (오늘 날짜 아님 — 지침 범위 밖)**

| 날짜 | 항목 | 상태 |
|---|---|---|
| 2026-08-08 | [A] v2 미완 18문항 이어하기 → n=50 완성 + README 착수 + Lv2 1문제 | 시작 전 (그대로) |
| 2026-08-09 | [B] k=50 pure_budget EM 측정 + recall 곡선 그래프화 + README + Lv2 1문제 | 시작 전 (그대로) |

두 건 다 실제로 미착수이므로 상태가 실태와 일치한다. 손대지 않았다.

### 푸시

미실행. 검토 후 `quick-push.ps1`로 진행 예정.
**커밋 대상은 TIL 2개(`til/2026-08-09.md`, `til/2026-08-10.md`)뿐이다.** `git status`의 83개 파일은 줄바꿈 차이이므로 이번 커밋에 섞지 말 것.
