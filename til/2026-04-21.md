# 2026-04-21 TIL

## 오늘 한 것
- 오늘은 code/ 폴더에 새로운 변경 없음
- Notion 캘린더 계획: requests + BeautifulSoup 크롤러 튜토리얼 + 프로그래머스 3문제 → 미착수
- (참고) 어젯밤 늦게 작업한 파일이 4/20 TIL에 미반영되어 있음:
  - `chapter6.Python_programming.py` — 점프투파이썬 6장 프로그래밍 문제 4개 풀이 (3과 5의 배수 합, 게시판 페이징 함수, 숫자 야구 게임, 시저 암호)
  - `pg_lv0_065.py` — 세로 읽기 (슬라이싱 `my_string[c-1::m]`으로 한 줄 풀이, 처음에 복잡하게 접근해서 시간 소모)
  - `pg_lv0_069.py` — 인덱스로 글자 지우기 (빈 문자열 치환 + join 풀이 vs `sorted(reverse=True)` + `del` 풀이 비교)

## 배운 개념
- (어젯밤 작업 기준) `random.sample(range(1,10), 3)` — 중복 없는 랜덤 숫자 추출
- (어젯밤 작업 기준) `ord()` / `chr()` 활용한 알파벳 시프트 연산 (시저 암호)
- (어젯밤 작업 기준) 슬라이싱 스텝 활용: `s[start::step]`으로 등간격 문자 추출 가능
- (어젯밤 작업 기준) 리스트에서 인덱스로 삭제 시 `sorted(reverse=True)` 후 `del` → 앞쪽 인덱스 밀림 방지

## 막힌 부분
- (비워둠 - 직접 채움)

## 내일 할 것
- 크롤러 백지 재현 + 기능 2개 추가 + 프로그래머스 3문제 (Notion 캘린더 기준)
- 오늘 미진행분(requests + BeautifulSoup 크롤러 튜토리얼 + 프로그래머스 3문제)도 함께 소화 필요
