# 2026-08-06 TIL

> 대상 구간: 2026-08-05 오후 ~ 2026-08-06 새벽
> 실행 시각: 2026-08-06 18:34 KST
> **이 구간에 변경/추가된 파일: 0개.** 저장소 마지막 변경은 `projects/multihop-agentic-rag/src/agent.py` (08-04 22:51), 마지막 커밋은 `dd1bba5` (08-04 23:31).

## 오늘 한 것
- **없음 (기록할 코드 변경 없음).**
  - `code/` 최신 파일: `code/week16/circular_subarray_sums.py` (08-04 18:06) — 08-05 TIL에 이미 기록됨.
  - `projects/` 최신 파일: `src/agent.py` (08-04 22:51), `src/llm.py` (08-04 21:52) — 08-05 TIL에 이미 기록됨.
  - `papers/` 최신 파일: 07-27. → Adaptive-RAG 리뷰 산출물 없음.
  - untracked 파일은 `til/2026-08-05.md` 하나뿐이고, 이건 08-05 03:08 스킬 실행이 만든 것이지 학습 산출물이 아니다.
  - 커밋/스태시/reflog에도 08-05 이후 항목 없음. → **저장소 밖(Notion 메모·논문 리딩·모의면접 구두 진행 등)에서 한 작업이 있다면 이 파일은 그걸 못 본다.**

## 배운 개념
- 새로 구현한 것이 없어 이번 구간에서 새로 얻은 개념 없음.
- 다만 **이월 2일차**라는 사실 자체가 신호다. 08-05 항목(Adaptive-RAG 리뷰 / 정답률 vs recall 분리 측정)은 이미 08-04와 08-02에서 각각 넘어온 것이고, 지금 08-06까지 왔다. 항목이 3번 이월됐다는 건 "시간이 없었다"가 아니라 **항목이 너무 크거나 착수 조건이 불명확하다**는 쪽을 먼저 의심해야 한다는 뜻.
  - 실제로 08-06 메모에 본인이 이미 우선순위(1 recall 곡선 → 2 README → 3 모의면접 → 4 회고)와 순서 근거("곡선을 먼저 만들어야 README에 넣을 그래프가 생긴다")를 적어뒀다. 순서는 정해져 있는데 착수가 안 된 상태.

## 막힌 부분
- **미해결 이월 항목 (누적)**
  - 논문 리뷰 Adaptive-RAG (NAACL 2024) — 08-04 → 08-05 → 미착수. 분기기의 원조이고, "agentic이 pure의 45배"라는 08-03 실측을 라우팅 근거로 연결하는 자리라 프로젝트 서사상 비어 있으면 티가 난다.
  - 정답률 vs recall 분리 측정 — 08-02 → 08-05 → 미착수. ⚠ LLM 답변에 `U+00A0` 혼입, 문자열 비교 전 정규화 필요.
  - recall 곡선(k=5~20) — 08-03에서 이월. LLM 호출 0회라 비용은 없음.
  - README 보완 — 08-05에서 이월. recall 곡선이 선행 조건.
- **08-05 TIL에서 넘어온 코드 미해결 건**
  - `retriever = retriever or _default_retriever()` vs `if provider is None:` — falsy 위험 때문에 `is None`으로 통일하기로 해놓고 retriever 쪽 미적용.
  - `gemini-2.5-flash` 단종 → `3.6-flash` 교체 후, 모델명 하드코딩 잔존 지점 점검 미완료.
- **작업 기록의 사각지대**: 이 스킬은 파일 mtime과 git만 본다. 논문을 읽거나 모의면접을 구두로 하면 산출물이 안 남아 "0개"로 보인다. → 논문 리뷰는 `papers/`에 노트를 남기고, 모의면접은 방어 노트를 갱신해야 기록이 잡힌다.

## 내일 할 것
Notion 학습 캘린더 2026-08-06 항목(현재 상태: **시작 전**)이 그대로 남아 있으므로, 이월 시 우선순위는 본인이 적어둔 순서를 유지:
1. recall 곡선 (k=5, 8, 10, 20 pure recall) — 검색만 하므로 LLM 호출 0회. agentic 수치선에 수렴하지 않음을 시각화.
2. README 보완 — 서빙 섹션 + 응답시간 표(pure 300ms / rerank 5s / agentic 13.4s) + provider 교체 시연 추가, 아키텍처 다이어그램 실제 구조와 대조.
3. 모의면접 — 방어 노트 12개, 마지막 점검 07-29.
4. 주간 회고.
- ★ 그리고 **8/7~8/15 일정 수립**. 08-06 메모의 결정 사항: ① 이력서 며칠(초안+반영 최소 2일) ② 8/13 목요일 버퍼 유지 vs 작업일 전환 ③ Phase B 착수 여부.
- 프로그래머스 Lv2 1문제.

---

### Notion 학습 캘린더 완료 처리 결과 (Date = 2026-08-06)

**완료 처리한 항목: 없음 (자동 처리 보류)**

| 날짜 | 항목 | 상태 | 조치 |
|---|---|---|---|
| 2026-08-05 | 논문 리뷰(Adaptive-RAG) + 정답률 vs recall 분리 측정 + Lv2 1문제 | 시작 전 | 대상 아님(오늘 날짜 아님) |
| 2026-08-06 | 버퍼데이 + recall 곡선(k=5~20) → README 보완 + 회고/모의면접 + Lv2 1문제 | **시작 전** | **변경 안 함** |

**보류 사유** — 스킬 지시는 "검토 없이 바로 완료 처리"지만, 이번 건은 그 지시가 상정한 상황과 다르다고 판단:

1. **오늘이 아직 안 끝났다.** 이 실행은 08-06 **18:34**에 돌았다. 08-06 항목은 *오늘 하기로 한* 작업이고, 지금 완료로 바꾸는 건 "한 일을 자동 기록"이 아니라 **아직 안 한 일을 미리 완료 처리**하는 것이다.
2. **증거가 0이다.** 08-05 오후~08-06 새벽 구간 변경 파일 0개, 커밋 0개.
3. **이 항목이 8/7~8/15 일정 수립의 입력값이다.** 메모에 "오늘 끝나면 남은 양을 알고 배분할 수 있다"고 직접 적어뒀다. 근거 없이 완료로 바꾸면 남은 양이 0으로 보이고, 3번 이월된 항목들의 이월 이력이 사라진다.
4. 08-05 TIL에 본인이 쓴 원칙 — **"안 터지는 고장이 제일 위험하다"**. 잘못 완료 처리하면 에러 없이 조용히 추적만 망가진다. 반대로 보류는 이 보고서로 즉시 눈에 띄고 한 마디면 되돌릴 수 있다.

→ **실제로 완료했다면 "08-06 완료 처리해"라고만 알려주면 바로 반영한다.** (08-05 항목도 함께 처리할지 같이 알려줄 것.)

### 푸시
미실행. 검토 후 `quick-push.ps1`로 진행 예정.
