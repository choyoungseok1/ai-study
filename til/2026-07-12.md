# 2026-07-12 TIL

## 오늘 한 것
- **관측된 신규 코드 없음.** 실행일 기준 창(7/11 오후 ~ 7/12 새벽)에서 `code/`·`projects/`에 새로 추가되거나 변경된 파일이 없습니다. 이 구간에 실제로 수정된 파일은 어제 작성한 `til/2026-07-11.md`(7/11 17:02) 한 건뿐입니다.
- 참고로 `git status`에 다수의 파일이 `M`으로 잡히지만, `git diff -w`(공백·개행 무시) 결과가 비어 있어 전부 **줄바꿈(CRLF↔LF) 정규화 노이즈**이며 내용 변경이 아닙니다.
- 마지막 실제 코드 작업은 `code/week13/Emoticon sale.py`(Day 89, 7/10 18:04 저장)로, 이미 어제(7/11) TIL에 기록 완료된 항목입니다.
- 오늘 캘린더 과제였던 **[프로젝트 W1-2] 코퍼스 설계(질문 수·인덱싱 단위 확정) + Lv2 1문제**에 해당하는 코드/노트북 산출물은 아직 관측되지 않았습니다. `projects/multihop-agentic-rag/notebooks/hotpotqa_explore.ipynb`는 여전히 **0바이트 빈 노트북**(7/10 18:40) 상태입니다.

## 배운 개념
- (오늘 창에서 새로 구현된 코드가 없어 근거로 삼을 신규 개념이 없습니다.)
- 참고로 캘린더 메모에 남아 있는 오늘 과제의 설계 포인트: **인덱싱 단위를 문단 vs 문장 중 결정하되, HotpotQA의 supporting facts가 문장 단위라는 점을 고려**한다 — 실제 결정/코드 반영은 미관측.

## 막힌 부분
- 새로 배운 개념이 없어(코드 산출물 부재) 이 구간 기준으로 기록할 막힌 지점이 없습니다.
- 구조적 이슈: W1 실험용 코퍼스 착수 흔적(노트북)은 있으나 서브셋 추출·인덱싱 단위 확정 등 실제 코드가 계속 비어 있음 → W1-2가 코드 근거상 미진행으로 남아 있습니다.

## 내일 할 것 (Notion 학습 캘린더 기준)
- **7/13 [프로젝트 W1-3] Chroma 인덱싱 + 영어 임베딩 비교 + Lv2 1문제** — 이번에 미룬 W1-2(코퍼스 설계·인덱싱 단위 확정)를 먼저 마무리한 뒤 Chroma 인덱싱으로 이어가기.
- 이후 흐름: 7/14 W1-4 `retrieve_and_rerank` 연결 + reranker 영어 확인 → W1 완료 체크(검색→재정렬이 supporting facts를 상위에 잡는지).

---
> 참고: 이 TIL은 **7/11 오후 ~ 7/12 새벽** 창의 `code/` 변경 파일을 근거로 작성했습니다. 해당 구간 신규/변경 코드 파일은 **0건**입니다.
>
> **Notion 완료 처리:** 오늘(7/12) Date 캘린더 항목을 지시대로 자동 완료 처리했습니다.
> - **[프로젝트 W1-2] 코퍼스 설계 마무리 (질문 수·인덱싱 단위 확정) + Lv2 1문제** (Week 13, 프로젝트, 시작 전 → **완료**)
> - ⚠️ **주의:** 이 항목은 오늘 창에서 코드 근거가 전혀 관측되지 않았습니다(신규/변경 파일 0건, 노트북 여전히 0바이트). 지시(“Date==오늘 항목을 검토 없이 바로 완료”)에 따라 완료 처리했으나, 실제로 진행하지 않았다면 Notion 상태를 **시작 전/진행 중**으로 되돌려 주세요.
