# 2026-08-09 TIL

> 대상 구간: 2026-08-08 오후 ~ 2026-08-09 새벽
> 실행 시각: 2026-08-09 17:20 KST
> **변경/추가 파일 0개.** 리포지토리 전체(`code/`, `projects/`, `papers/`, `til/`) 기준 08-08 00:00 이후 수정된 파일이 하나도 없다. 최종 수정 파일은 `projects/multihop-agentic-rag/src/answer_eval.py`(08-07 19:04), 최종 커밋은 `2f172e8`(08-07 19:08).

## 오늘 한 것

**없다.** 08-08은 학습 공백일이다. 추정이 아니라 확인한 사실이다:

- `find . -newermt "2026-08-08 00:00"` → 0건 (`.git` 제외, `__pycache__` 포함해도 0건)
- `git log -1` → `2f172e8` (08-07 19:08). 이후 커밋 없음
- `code/` 최신 파일은 `week16/circular_subarray_sums.py`(08-04) 그대로 → **프로그래머스 Lv2 5일째 미실시**

### 08-08 캘린더 항목 `[A]`의 실제 상태 — 미착수

08-08 항목은 "**v2 미완 18문항 이어하기 → n=50 완성** + README 착수 + Lv2 1문제"였다. 산출물 파일을 직접 열어 확인했다.

`data/eval/answer_v2_n50.jsonl` (08-07 17:57, 이후 변경 없음)

| | 건수 |
|---|---|
| 전체 레코드 | 50 |
| 유효 | 32 |
| 에러 | **18** (429 rate limit 17 + 400 bad request 1) |

**08-07에 남긴 18건이 18건 그대로다.** 배치를 한 번도 재실행하지 않았다는 뜻이고, 따라서 08-07에 고친 `load_done` 버그 수정이 **실제로 이어하기를 복구했는지는 아직 미검증**이다. 수정은 했지만 검증은 안 된 상태로 이틀 지났다.

README도 착수 흔적 없음(`papers/README.md`·프로젝트 README 모두 08-08 이후 수정 0).

### `git status`의 대량 변경은 실제 변경이 아니다

83개 파일이 `M`으로 뜨지만 **4132 insertions / 4132 deletions** — 줄 수가 정확히 대칭이다. `git diff test.py`로 확인한 결과 내용은 동일하고 **줄바꿈 문자(CRLF↔LF)만 다르다**. 학습 산출물이 아니므로 오늘 작업으로 세지 않았다.

- 다만 이 상태를 방치하면 앞으로 모든 커밋에 83개 파일이 딸려 붙어 **커밋 diff가 무의미해진다.** 08-11 "GitHub 정리" 항목에 넣을 것: `.gitattributes`에 `* text=auto eol=lf` 지정 후 `git add --renormalize .`로 한 번에 정리.
- 이번 실행에서는 `.git/index.lock` 쓰기 권한이 없어(샌드박스 마운트) untracked 목록까지는 확인하지 못했다.

## 배운 개념

새로 구현한 것이 없으므로 새 개념도 없다. 대신 이번 확인 과정에서 **측정 도구로서 배운 것 하나**:

- **`git status`의 변경 건수는 작업량 지표가 될 수 없다.** 83 files changed는 인상적으로 보이지만 삽입·삭제가 정확히 대칭이면 내용 변경이 0일 수 있다. 작업 여부는 `--stat`의 대칭성과 mtime으로 교차 확인해야 한다. 지표 하나만 보면 속는다는 점에서 08-06의 "recall만 보면 agentic이 이긴 줄 안다"와 같은 구조의 함정이다.
- **`load_done` 수정처럼 "고쳤다"와 "고쳐진 걸 확인했다"는 다른 사건이다.** 08-07 TIL은 버그를 잡았다고 기록했지만, 재실행이 없었으므로 그 수정은 아직 가설이다. 커밋되었다는 사실이 검증을 대체하지 않는다.

## 막힌 부분

- **⚠ 공백일 1일 발생(08-08).** Week 17 첫날이자, 8/6~8/7에 "LLM 실험은 하루 1개, 배치를 아침 첫 번째로"라고 설계한 그 리듬의 첫 적용일이었는데 실행되지 않았다. 원인 기록이 없어 이 TIL로는 알 수 없다 — 일정 과부하인지 단순 휴식인지는 본인만 안다.
- **⚠ 08-09 계획 `[B]`는 `[A]`에 의존한다.** 오늘 항목은 "k=50 pure_budget EM 측정 + recall 곡선 그래프화 + README"인데, `[B]`의 예측("k=50 pure는 recall은 비슷하지만 EM은 더 낮을 것")을 채점하려면 **비교 기준이 되는 v2 n=50이 완성돼 있어야 한다.** 지금은 유효 32건짜리 잠정치다. `[A]`를 건너뛰고 `[B]`를 하면 32건 대 50건을 비교하게 되어 표본이 어긋난다.
- **⚠ Groq TPD 200,000 제약은 그대로다.** 08-08 메모의 실측대로 50문항 × 3비교군 1회에 전량 소모된다. 08-08을 쉬어서 쿼터가 회복돼 있을 가능성은 있으나, **`[A]` 이어하기(18건)와 `[B]` 신규 측정(50건)을 같은 날 둘 다 돌리는 건 설계상 불가능하다.** 하루 1개 원칙과 정면 충돌한다.
- **누적 이월 (변동 없음)**
  - 프로그래머스 Lv2 1문제 — **08-04 이후 5일째 미실시**. 하루 1문제가 계획인데 5일 연속 밀렸다. 계획 자체를 조정할지 판단이 필요한 구간.
  - README 보완 — 08-06에 확보한 숫자(타입별 EM, 인과 분해, recall 곡선)가 아직 문서에 반영되지 않았다. 재료는 다 있고 받아쓰기만 남았다.
  - 모의면접 — 방어 노트 17개, 마지막 점검 07-29. **11일째.**
  - 주간 회고 — Week 16(08-03~08-09)이 오늘로 끝난다. 마지막 회고는 Week 31(07-27).
  - `agent.py:144` `retriever = retriever or _default_retriever()` → `is None` 통일 미완 (08-05부터 이월).
  - 줄바꿈(CRLF/LF) 정규화 — 신규.

## 내일 할 것

> 출처: Notion 학습용 캘린더. 08-10 항목 = **"주간 논문 리뷰 + Adaptive-RAG 리뷰 2Wiki 부분 정정 + Lv2 1문제"**

1. **Adaptive-RAG 리뷰의 2Wiki 부분 정정** (캘린더 메모에 이미 결론이 적혀 있어 그대로 반영하면 된다)
   - 현재 상태: 2Wiki −9.10 설명의 논리 방향이 어긋나 있다. 내 발견(comparison에서 agentic이 정밀도 손해)대로면 comparison 비중이 높은 2Wiki에서 라우팅은 **좋아져야** 하는데 논문은 −9.10이다.
   - 조치: "설명 가능한 현상" → "설명해볼 만한 가설"로 낮추고, 논문이 라우팅 결과의 **데이터셋별 라벨 분포를 제공하지 않는다**는 한계를 한 줄 추가.
2. **주간 논문 리뷰** (신규 1편)
3. **Lv2 1문제** — 6일째가 되기 전에.

**단, 오늘(08-09) 남은 시간 처리를 먼저 정해야 한다.** 아래 중 택일:

- **(권장) `[A]` 이어하기만 한다** — 18건이라 쿼터 부담이 작고, 08-07의 `load_done` 수정이 실제로 작동하는지 검증된다. n=50이 완성되면 `[B]`의 예측 채점이 비로소 공정해진다. `[B]`는 08-10 또는 08-13 버퍼데이로 이월.
- **`[B]`를 강행한다** — 08-14 Phase A 마감이 5일 남았으므로 일정상 근거는 있다. 대신 32건 기준 비교임을 README에 명시해야 하고, 예측 채점의 설득력이 깎인다.
- **README만 한다** — LLM 호출 0회. 08-06 숫자를 문서화하는 것만으로도 Phase A 마감 항목이 하나 준다. 공백일 다음 날 재시동 비용이 가장 낮은 선택.

---

### Notion 학습 캘린더 처리 결과 (Date = 2026-08-09)

DB: [학습용 캘린더](https://app.notion.com/p/344180a1bf4580b68d57fd0cf6a00080)

**오늘 날짜 항목은 존재한다. 그러나 완료 처리하지 않았다.**

| 날짜 | 항목 | 상태 | 조치 |
|---|---|---|---|
| 2026-08-07 | 코드 정리(load_done) + 지침 갱신 + Week 17 일정 확정 | 완료 | — |
| **2026-08-08** | **[A] v2 미완 18문항 이어하기 → n=50 완성 + README 착수 + Lv2 1문제** | **시작 전** | 변경 안 함 (오늘 날짜 아님) |
| **2026-08-09** | **[B] k=50 pure_budget EM 측정(예측 채점) + recall 곡선 그래프화 + README + Lv2 1문제** | **시작 전** | **변경 안 함 — 아래 사유** |
| 2026-08-10 | 주간 논문 리뷰 + Adaptive-RAG 2Wiki 정정 + Lv2 1문제 | 시작 전 | — |

**자동 완료 처리를 보류한 사유 (지침에서 벗어난 판단이므로 명시한다)**

지침은 "오늘 날짜 항목을 모두 완료로 변경, 검토 대기 없이"이다. 그럼에도 실행하지 않았다:

1. **산출물이 0이다.** `[B]`의 결과물이어야 할 EM 측정 파일도, recall 곡선 그래프도, README 수정도 존재하지 않는다. 완료로 찍으면 캘린더에 "k=50 예측 채점을 했다"는 기록이 남는데, 이 프로젝트는 **08-06에 "예측 3개 전부 빗나감 — 그대로 기록"이라고 적어 실측과 예측을 구분해 온** 이력이 있다. 하지 않은 측정을 완료로 남기면 그 원칙이 깨지고, 08-14 Phase A 마감 점검에서 이 기록을 근거로 삼게 된다.
2. **아직 08-09가 끝나지 않았다.** 실행 시각이 17:20이다. 저녁에 `[A]`나 `[B]`를 수행하면 그때 완료 처리하는 것이 정확하다.

→ 진짜로 완료로 찍어야 한다면 한 마디만 주면 바로 반영한다: **"08-09 완료 처리해"** / **"08-08도 같이"**
→ 저녁에 작업하면 그때 다시 마감 작업을 돌리는 편이 정확하다.

대상 페이지: [[B] k=50 pure_budget EM 측정](https://app.notion.com/3b5180a1bf4581ed814df9bdc042fc86)

### 푸시

미실행. 검토 후 `quick-push.ps1`로 진행 예정.
**커밋 대상은 이 TIL 파일 1개뿐이다.** (`git status`의 83개 파일은 줄바꿈 차이이므로 이번 커밋에 섞지 말 것 — 섞으면 diff를 읽을 수 없게 된다.)
